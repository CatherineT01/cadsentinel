# cadsentinel Python ETL requirements
# Install with: pip install -r requirements.txt

# Database
psycopg2-binary>=2.9.9

# Embedding API (OpenAI-compatible)
openai>=1.30.0

# Testing
pytest>=8.0.0

# Optional: for local embedding models via LiteLLM proxy
# litellm>=1.40.0